---
# Pure GitOps Bootstrap - Only install K3s infrastructure
# All applications will be deployed via ArgoCD

# Step 1: Base OS tuning
- import_playbook: pi-setup.yml

# Step 2: Install k3s (multi-master + workers)
- import_playbook: k3s-setup.yml

# Step 3: Install ArgoCD for GitOps management
- import_playbook: argocd-bootstrap.yml
