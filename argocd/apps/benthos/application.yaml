apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: benthos
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Sam1rShaban1/k3s-iot-stack
    targetRevision: main
    path: manifests/benthos
  destination:
    server: https://kubernetes.default.svc
    namespace: benthos
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
